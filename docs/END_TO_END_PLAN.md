# End-to-End Plan â€” Site Tracker Pro (research-driven)

> Grounded in `docs/research/01_CHAT_SOURCE.md`. Executed via the loop in `docs/AGENTIC_SDLC.md`.
> Legend: âœ… shipped Â· ðŸŸ¡ partial / verify Â· â¬œ gap / future.

## 1. Product Vision (from research)

One multi-tenant core platform (CRM, Projects, Permission engines) + industry plugins (Construction / Architecture / Interior / Consultant), chosen at company onboarding via toggles + workflow templates. Killer feature: **Client Approval & Revision System** (Figma-style drawing comments with x/y anchors, share links, version locking, digital signature). Later: white-label subdomains, mobile app, AI, analytics.

## 2. Research â†’ Current State Map

| Research module | Repo status | Evidence / next |
|---|---|---|
| Multi-tenant orgs + `company_id` everywhere + RLS | âœ… | organizations, org_id, RLS policies |
| Auth / roles / user_roles / RBAC | âœ… | capabilities, user_roles, org roles |
| CRM & Sales (leadsâ†’meetingsâ†’quotationsâ†’agreements) | âœ… | CRM phase A |
| Client Management | âœ… | /clients, client profiles |
| Design Studio | âœ… | design_workflow, drawings register, diff overlay, FFE |
| **Drawings revisions + versions** | âœ… | drawings register + parent_id revisions + version diff (B1) |
| **Figma-style comments (x, y pin)** | âœ… | drawing_comments with x/y anchors + status threads (migration 185) |
| **Client share link + approval + final lock** | âœ… | validate_share_link / share_project_payload RPCs + /share-link/:token + approval lock |
| **Digital signature on approval** | âœ… | SignaturePad captured into handover_signatures on approval |
| BOQ & Estimation | âœ… | BoqTab, EstimateTab |
| Site Execution (milestones, tasks, daily progress, photos) | âœ… | Sprint 1 |
| DPR | âœ… | Sprint 2 DPR module |
| Site Supervision (inspections, checklist) | âœ… | G2 inspections + corrective actions |
| Labour (wages, attendance, shifts, statutory) | âœ… | G3 shift roster, overtime, EPF/ESI |
| Materials (requests, GRN, inventory) | âœ… | G1 |
| Finance & Billing (milestones, invoices, RA bills, retainer/hourly) | âœ… | finance module |
| Notifications (in-app / email / WhatsApp) | ðŸŸ¡ | in-app exists; verify email/WhatsApp |
| Documents / file service (DWG/DXF/SKP/RVT preview) | ðŸŸ¡ | storage buckets exist; CAD preview â¬œ |
| Client Portal | âœ… | ClientPortalView + /client/:projectId depth (payments, milestones, drawings, feed) |
| Handover (checklist, completion cert, signature) | âœ… | HandoverPacketView |
| Reports & Analytics | âœ… | ReportsPage + approval analytics (/approval-analytics) |
| Core + plugins architecture | âœ… | src/plugins/catalog.ts lazy routes, ModuleGate |
| Onboarding: industry toggle + module toggle + templates | âœ… | INDUSTRY_TEMPLATES, enabled_modules |
| Feature flags | âœ… | feature_flags / enable_* |
| Subscription plans (Basic/Pro/Enterprise) | âœ… | plans, PlanGate, usage-limit enforcement (QuotaGate/useFeatureWithQuota) |
| White-label (per-org branding) | âœ… | per-org branding (Phase F) |
| White-label subdomains | â¬œ | future |
| Mobile app / AI / voice comments / 3D annotations | â¬œ | future (research V2) |
| Superadmin platform panel | âœ… | Track A complete (SA-F â†’ SA-T, incl. migration 184 live) |

## 3. Track A â€” Superadmin Platform Panel (complete)

| Phase | Scope | Status |
|-------|-------|--------|
| SA-F | Capability matrix + audit consolidation | âœ… `73d3d37` |
| SA-D | Platform dashboard rebuild | âœ… `73d3d37` |
| SA-O | Organizations screen rebuild (MRR, plan mix, CSV) | âœ… `a09d7f8` |
| SA-U | Users & Staff screen rebuild (migration 184 `platform_users` live) | âœ… `d8c8c50` `b1575f7` `f3ed4d1` |
| SA-AR | Active Requests / platform support screens | âœ… `ede4f3d` |
| SA-S | Subscription & billing platform screens | âœ… `8d0cb11` |
| SA-T | Testing + ship (db:apply 171 passed, prod push, live 200) | âœ… |

## 4. Track B â€” Research-Gap Product Roadmap

- **B1 â€” Client Approval & Revision System (killer feature).** Drawing versions register per drawing; Figma-style comments with x/y anchors + comment threads (Open/In Progress/Resolved/Closed); share links with password/OTP/expiry/download restriction; approve/reject + final lock; digital signature capture; revision timeline; notifications to architect; approval analytics (avg approval time, approval %, rounds).
- **B2 â€” Client Portal depth.** Payments view, upcoming milestones, approved drawings, comment surface for clients, activity feed.
- **B3 â€” Subscription & limits enforcement.** Enforce Basic/Pro/Enterprise usage limits (users, projects, modules, storage); upgrade gate UX.
- **B4 â€” Notifications.** Email/WhatsApp delivery for comment/revision/approval events (in-app done).
- **B5 â€” Storage & documents.** CAD/DXF/SKP preview, versioned file handling, storage quota usage.
- **B6 (future).** White-label subdomains; mobile app; AI features (floor-plan suggestions, auto change detection, voice comments); advanced analytics.

### 4.1 Track B execution status

| B item | Status | Details |
|--------|--------|---------|
| B1 â€” Client Approval & Revision | âœ… shipped (code) + **DB substrate now live** | Commit `39972e7` (B1). Frontend: `DrawingReviewTab` (x/y comment pins, status-ladder threads, share-link manager), `ShareLinkView` at `/share-link/:token`, `ApprovalAnalyticsView` at `/approval-analytics`, `SignaturePad`, `approvalQueries.ts`. DB: migration 185 (`drawing_comments`, `share_links`, `handover_signatures`, `drawings.parent_id/change_note/approval_status/approved_by/signature/author_id`), RPCs `validate_share_link`/`share_project_payload`/`create_share_link`, 187 triggers. **185 had 3 bugs (SRF-in-WHERE, grants-before-definition, `limit` inside `jsonb_agg`) â€” fixed in `e9c5889`, applied live, verified (23/23 probe).** |
| B2 â€” Client Portal depth | âœ… shipped + verified | Commit `6b6963c` (B2). `ClientPortalProjectView` at `/client/:projectId` (payments rollup, upcoming milestones, approved drawings, activity feed, invoices), `clientPortalQueries.ts`, `ClientDashboard`/`ClientPortalView` list surfaces. Live schema verified (`plan_cap`, `org_quota_snapshot`). |
| B3 â€” Usage-limit enforcement | âœ… shipped + verified | Commits `916c56e`+`deacd3c` (B3). `QuotaGate`/`QuotaMeter`, `useFeatureWithQuota`, quota meters in OrgBilling/PlatformBilling/PlatformUsage, gates in CreateProject/InviteMember. Live `plan_cap`/`org_quota_snapshot` RPCs present. |
| B4 â€” Notifications | ðŸŸ¡ partial â†’ **email âœ…** | B4 (WhatsApp share in DPR) `b70e7a2`, B4.3 (email templates + mock SES) `2793f6b`, B4.4 (notification prefs UI) `9472140` â€” **`toggleNotifType` bug fixed in `e9c5889`**, B4.5 (org broadcast RPC + UI) `2eb64b1` â€” migration 188 `send_org_notification` had 4 bugs (DEFAULT-in-RETURNS-TABLE, JS-array refs, 2-col SELECT INTO, `pm.status`) â€” **fixed in `e9c5889`, applied live.** **Real EMAIL delivery now LIVE (2026-08-15)**: migration **198** `notify_delivery_wiring` + `scripts/configure-notify-delivery.mjs` + redeployed `notify-deliver` EF **v18 `verify_jwt:false`** + EF `fetchProfile` email fix (`auth.admin.getUserById`) â€” verified end-to-end live (`delivered_at` set via Resend). WhatsApp/Twilio/push still deferred (no provider secrets). |
| B5 â€” Storage/CAD preview | ðŸŸ¡ partial | `afd2254` dropped the redundant B5 drawing-comments; storage buckets + download audit exist; DWG/DXF/SKP preview â¬œ. |
| B6 â€” White-label branding | âœ… | `7bc3762` (B6 org branding) + `afd2254` build repair. Subdomains/mobile/AI â¬œ. |

### 4.2 B1â†’B2â†’B3 live-apply fix (2026-08-14, commit `e9c5889`, prod live 200)

- **Root cause**: B1/B2/B3 code was shipped earlier but the DB substrate (migrations 185â€“188) **never applied live** â€” 185 and 188 had real PostgreSQL errors, and 186/187 cascaded off 185's aborted transaction.
- **185 fixes**: `d.project_id = any(public.user_project_ids())` â†’ `in (select public.user_project_ids())` (SRF-in-WHERE illegal â€” `user_project_ids()` returns `setof uuid`; `user_org_ids()` returns `uuid[]` so its `= any(...)` usages are fine); added `drawings.author_id` (187's triggers need it); moved `validate_share_link`/`share_project_payload` anon grants after the function definitions; moved `limit 10` out of the `jsonb_agg(...)` call into a subquery.
- **188 fixes**: removed `DEFAULT` from `RETURNS TABLE` columns; `SELECT id, name INTO` â†’ `SELECT name INTO`; replaced nonexistent JS refs (`NOTIFICATION_TITLES`/`NOTIFICATION_BODIES`/`generateTitle`/`generateBody`) with the `notification_templates` row; `pm.status = 'active'` â†’ `pm.removed_at IS NULL`; removed the inverted `notification_prefs` skip; `:=` instead of `DEFAULT` in DECLARE.
- **Result**: `npm run db:apply` â†’ **175 passed / 1 failed** (only the benign pre-existing `120_seed_test_data` dev-seed). Live probe: drawing_comments/share_links/handover_signatures tables, all B1 columns, 3 RPCs + anon grants, 5 triggers (187), notification_templates/trigger_notify_deliver (186), send_org_notification (188), plan_cap/org_quota_snapshot (B2/B3) â€” all present.
- **Bonus fix**: `src/lib/notificationPrefs.js` `toggleNotifType` referenced `activeRole` (undefined in that scope â†’ runtime `ReferenceError` on toggle); now uses the `role` param.

## 4.3 Track C â€” Live DB drift audit + health sweep (2026-08-15)

> Loop: per sub-task Deep-Dive â†’ Plan â†’ Build â†’ Verify â†’ commit, phase re-check â†’ testing â†’ push live. Same loop as AGENTIC_SDLC.md.

| C item | Status | Details |
|--------|--------|---------|
| **C1 â€” Missing table grants** | âœ… `7aa1ea6` (migration 189, applied live, prod 200) | **Root cause**: 18 tables had RLS policies but `authenticated` was never GRANTed table-level DML â†’ PostgREST `permission denied for table X` on every direct query (grants are checked BEFORE RLS). Trigger surfaced via `platform_feature_flags` (the fixed `platformSettingsQueries` hit it next). Migration `24_feature_flags.sql` created the 3 flag tables with policies but no grants; the same gap silently affected 15 more. **Fix**: `189_auth_grants.sql` grants exactly the DML each table's existing policies permit (full DML where write policies exist, SELECT-only where read-only), revokes anon â€” mirrors the 72â€“78/131/137â€“179 grant pattern. Applied live + verified: `SET ROLE authenticated` queries now succeed on all 18 (was permission-denied), live probe confirms **0 of 99 directly-queried tables** lack an authenticated SELECT grant. `subscriptions` stays ungranted (only reached via the `orgs` view / RPCs â€” correct). |
| **C2 â€” Column/query drift audit** | âœ… migration 190 applied live (pending commit/push) | Same class of live bug as `dpr_messages.transcript`, `notifications.message`, `ops_toggles.id`: frontend `.select()/.insert()/.update()` column names that don't exist on the live schema â†’ `PGRST204 column does not exist`. Automated every column token from `src/app/*Queries.ts` + views against live `information_schema` (temp probe scripts), triaged false positives (embed relations, alias flags, `audit_log_v2`/`ops_toggles`/`milestones`/`site_updates` verified correct, `invoice_lines` real). **Fix**: migration **190** adds intended-but-missing columns `invoices.due_date`, `ra_bills.due_date` (needed by live `check_overdue_payments()` in 176 + `paymentStatusFrom`), `organizations.contact_email` (onboarding + EF `index.ts`/`cashfree.ts`) â€” makes `crossAnalyticsQueries.getOrgCashFlowForecast` + `onboardingQueries` work with zero code change. Query rewrites to live schema: `hierarchyQueries` (blocks no `code` â†’ derived, floors `level`/no `project_id` â†’ via block ids, units `unit_code`/`unit_type`, no progress), `vendorPortalQueries` (`po_no`/`created_date`/`rate`), `shareQueries` (drawings `release_date` + `status='current'` + `storage_path` â€” live CHECK has no `released`), `clientPortalQueries.listClientInvoices` + `crossInvoiceQueries.listOrgInvoices` (polymorphic `payments` fetch via `target_type`/`target_id`, `received_on`, order by `issued_date` â€” the `payments!invoice_id` embed is invalid live). Verified live: migration 190 columns present; full gate green (tsc Â· vitest 2284 Â· smoke 378 Â· build Â· lint baseline-only). |
| **C3 â€” RPC signature/EXECUTE audit** | âœ… migration 191 applied live (pending commit/push) | Automated diff of all 65 frontend `.rpc()` call names + arg keys against live `pg_proc` signatures + `has_function_privilege`. 64/65 exist with EXECUTE granted to `authenticated` â€” **one real drift**: `recompute_all_vendor_performance` had **no live function** (migration 178 disabled it, "uuid[] issue", but `VendorScorecardView` "Recompute All" still calls it â†’ PGRST202 every click). The disabled body also had a positional bug (passed `project_id` into the `date` `p_period_start` slot). **Fix**: migration **191** re-implements it (previous-month period, correct arg order to `recompute_vendor_performance`, `security definer`, `grant execute` to authenticated, revoke anon/public), applied + verified live (auth_exec=true, functional probe runs clean). |

## 4.4 Pending Items â€” Agentic Loop Execution Plan (2026-08-15)

> **Method**: per phase, take one sub-task at a time â†’ Deep-Dive â†’ Plan â†’ Build â†’ Verify â†’ commit; finish ALL sub-tasks in a phase â†’ phase re-check â†’ next phase (same loop); after all phases â†’ re-check â†’ testing loop (same loop) â†’ release â†’ push live. User is unavailable; Lead (this session) takes all decisions.
> **Branch rule**: commit on `prod` â†’ push `origin prod` (auto Vercel deploy) â†’ verify live 200 â†’ fast-forward merge `prod` â†’ `main` â†’ push `origin main`. (Current: main = prod = `ef4e601`.)

| Phase | Scope | Sub-tasks | Status |
|-------|-------|-----------|--------|
| **P1 â€” Live-drift final sweep** | Verify the last unverified drift surfaces (repo vs live), record results. | P1.1 Edge Function deploy parity (9 frontend-invoked EFs) Â· P1.2 Storage buckets (deliverables / dpr-media / research-docs) Â· P1.3 Views/tables referenced by frontend | âœ… all 3 verified live (2026-08-15): **9/9 EFs ACTIVE** (`invite_org_member` v27, `remove_org_member` v5 recent); **3/3 buckets exist** with correct size limits (deliverables 50MB, dpr-media 15MB, research-docs 50MB; leftover `probe-c32` test bucket â€” harmless, note only); all frontend-referenced views/tables present in committed migrations (`orgs` 135, `wip_aging` 179, `vendor_performance` 178, `ops_toggles` 24). **No code/migration change required.** |
| **P2 â€” B5: CAD/DXF/SKP preview** | The last buildable Track-B gap: client-side preview for uploaded CAD files in the drawing/deliverable register. | P2.1 Deep-dive drawing storage + DrawingsTab/DeliverablesTab upload flow Â· P2.2 DXF text-entity parser (LINES/LWPOLYLINE/POLYLINE/CIRCLE/ARC/TEXT, zero-dep) Â· P2.3 SVG renderer + bounds/scale Â· P2.4 Preview modal + DWG/SKP graceful fallback (metadata + download prompt) Â· P2.5 Wire into DrawingsTab + DeliverablesTab Â· P2.6 Tests + smoke + commit | âœ… **complete `ef4e601`** (2026-08-15): `src/lib/dxfPreview.ts` (pure parser+SVG renderer, XML-escaped text, no DOM/client), `src/features/shared/CadPreviewModal.tsx` (full-size modal; DXF fetch-via-signed-URL â†’ parse â†’ render; DWG/SKP metadata+download fallback; loading/error+retry), eye-icon Preview button per CAD file row in DrawingsTab + DeliverablesTab (extension-gated, no capability change), 19 vitest tests (`tests/lib/dxfPreview.test.ts`). Also fixed pre-existing CI lint break (eslint test-block missing vitest globals â†’ `tests/email.test.js` 59 no-undef errors red on main). Gate: tsc âœ“ lint âœ“ vitest 194/2303 âœ“ smoke **382** âœ“ build âœ“ e2e-mock 11/11 âœ“. |
| **P3 â€” Testing loop** | Full regression with the same loop (unit â†’ integration â†’ smoke â†’ build â†’ e2e-mock). | P3.1 Full gate suite Â· P3.2 e2e-mock Â· P3.3 fix any surfaced issues via the loop | âœ… full gate green incl. **e2e-mock 11/11** (2026-08-15); surfaced + fixed the pre-existing CI lint break (see P2 row). |
| **P4 â€” Release** | Ship to live. | P4.1 commit/push `prod` Â· P4.2 verify live 200 + live probe Â· P4.3 FF-merge `prod` â†’ `main` â†’ push | âœ… **shipped `ef4e601`** (2026-08-15): push `origin prod` â†’ Deploy + CI green â†’ live https://sitetrackpro.in **200** â†’ FF-merge `prod`â†’`main` â†’ push. **Track B â€” B5 complete: storage + CAD preview done.** |

## 4.5 Track D â€” New Scope Phases (2026-08-15, user mandate)

> Method: one sub-task at a time â†’ Deep-Dive â†’ Plan â†’ Build â†’ Verify â†’ commit on `prod` â†’ push â†’ verify live â†’ FF-merge `main`. Loop repeats per phase. User unavailable; Lead takes all decisions.

| Phase | Scope | Sub-tasks | Status |
|-------|-------|-----------|--------|
| **P-A â€” Column/query drift: auth.users embeds** | 5 live PGRST200 breakages: PostgREST drops FKs targeting `auth.users` from the schema cache, so `*:fk(name)` embeds fail on every call. | A1 leads.owner_id (crmQueries Ã—3) Â· A2 payments.received_by (receiptQueries) Â· A3 po_receipts.received_by Â· A4 material_requests.requested_by+approved_by Â· A5 corrective_actions.opened_by(+verified_by) Â· A6 migration 192 FK re-point â†’ profiles Â· A7 live apply + REST probe + gate | âœ… **complete** (2026-08-15, migration **192** `fk_identity_to_profiles` applied live, verified): all 7 FKs `auth.users` â†’ `public.profiles` (same `ON DELETE SET NULL`; `profiles.id` is 1:1 with `auth.users.id`). Zero data risk (all columns empty, verified). Live REST probes: pre-fix **PGRST200** on all 5 embeds â†’ post-fix **42501** (anon legitimately lacks SELECT = embed now resolves). Full gate: tsc âœ“ lint âœ“ vitest 194/2303 âœ“ smoke 382 âœ“ build âœ“. |
| **P-B â€” Org project lifecycle** | Delete / pause / hold / deactivate / reactivate projects. | B1 lifecycle states (paused/on_hold/deactivated + reactivate + terminal archive) Â· B2 migration (status CHECK + quota) Â· B3 UI (list filter + tab gating + actions) Â· B4 tests + apply | âœ… **complete** (2026-08-15, commit `2341452`, migration **193** `project_lifecycle` applied live + Deploy/CI green + live 200): status CHECK extended `('active','paused','on_hold','deactivated','completed','cancelled')` + `idx_projects_org_status` partial index (applied + NOTICE-verified, 9 active live). `src/lib/projectLifecycle.ts` pure state machine (ladder: active â‡„ paused/on_hold/deactivated, â†’completed/cancelled terminal, reactivateâ†’active; tones/labels/live-project check). `queries.ts`: `ProjectSummary.archivedAt` + `getProject archived_at`, `setProjectStatus`/`archiveProject`/`restoreProject`/`deleteProject`. `ProjectsListView`: 8 lifecycle filter chips, tone badges, per-card actions menu (capability-gated `project:archive`/`restore`/`delete`), action error alert, refresh-after-mutation. `DetailView` header status badge. `status.ts` + `icons.tsx`: paused/deactivated/cancelled tones + `dots` icon. Tests `tests/app/projectLifecycle.test.ts` 12. Gate: tsc âœ“ lint âœ“ vitest 195/2315 âœ“ smoke 382 âœ“ build âœ“. |
| **P-C â€” Better project UI** | ProjectsListView redesign (search/filter/sort/stat strip/richer cards). | C1 inventory â†’ C2 layout + controls â†’ C3 wire queries â†’ C4 tests | âœ… **complete** (2026-08-15, commit `7b1114d`, Deploy + CI green + live 200): `ProjectSummary` extended with `progress/budget/startDate/expectedEndDate/clientName/description` (`listProjectsForOrg` + `getProject` select/map â€” additive for the 4 rollup consumers). New pure `src/lib/projectList.ts` (`projectRollup` lifecycle buckets + live budget, `filterProjects` name/location/client/description, `sortProjects` name/status/location/progress/budget/startDate asc/desc, `PROJECT_SORT_KEYS`). `ProjectsListView` redesign: 5-card stat strip (Live+budget / Active+paused-hold / Completed / Cancelled / Archived), search Input, sort Select + direction toggle, richer cards (progress bar + %, budget â‚¹, dates, client, 2-line description), keeps P-B lifecycle filter chips + capability-gated actions menu. Tests `tests/lib/projectList.test.ts` 14 (rollup/filter/sort incl. null-as-0, archived exclusion, no mutation). Gate: tsc âœ“ lint âœ“ vitest 196/2329 âœ“ smoke 382 âœ“ build âœ“. |
| **P-D â€” Zoho-style org signup onboarding** | Unify the two parallel org paths (/register vs /signup). | D1 deep-dive (done) â†’ D2 unified flow â†’ D3 UI + EF â†’ D4 tests | âœ… **complete** (2026-08-15, commit `9d73c93`, migration **194** `org_billing_period` applied live + Deploy/CI green + live 200): `/register` is now the single canonical Zoho-style self-service path. Migration 194 adds `organizations.billing_period` (monthly/annual, nullable for legacy â€” applied + pg-verified). `register_org` EF accepts + validates `billing`, stamps it on the org insert, and the welcome email now shows Plan (+ "annual â€” 2 months free") + Billing rows. `OrgRegisterView` gains a monthly/annual toggle (annual = "2 months free" badge, reuse of `signup.*` i18n keys), passes `billing` through, and the done screen shows the chosen cycle. `orgRegisterQueries` `RegisterInput.billing` + injectable client. Router: `/signup` â†’ `SignupRedirect` â†’ `/register` preserving `plan`/`billing` params; `SignupView.tsx` retired (deleted). `submitSignupRequest` EF + admin SignupRequestsView untouched (legacy/manual review). Tests `tests/app/orgRegisterQueries.test.ts` 6. Gate: tsc âœ“ lint âœ“ vitest 197/2335 âœ“ smoke 387 âœ“ build âœ“ e2e-mock 11/11 âœ“. |
| **P-E â€” Payment-at-signup + temp password + forced change** | Signup payment â†’ email temp password â†’ sign in â†’ mandatory new password. | E1 fix cashfree-webhook dead end (never marks signup paid) Â· E2 billing_history wiring Â· E3 temp-password email + admin-API user Â· E4 force password change Â· E5 tests | âœ… **complete** (2026-08-15, commit `f8998b8`, migration **195** `signup_payment_password` applied live + EFs deployed + Deploy/CI green + live 200): **E1** `cashfree-webhook` now handles Cashfree **PAYMENT_LINK_EVENTs** (the one-time signup payment from cashfree-checkout) â€” reconciles `signup_requests` via `link_meta.signup_request_id` or the stashed `payment_ref`, marks `payment_status='paid'` + `paid_at` + `paid_amount_paise` (from gateway order paise), ack 200 for orphan/idempotent/non-PAID, fire-and-forget Resend confirmation; subscription path unchanged. **E2** `review_signup_request` seeds `billing_history` (provider `cashfree`, `external_id=payment_ref`, `amount=paid_amount_paise`, `status='succeeded'`, `paid_at`) once the approved org exists (non-fatal). **E3** provisions NEW users via `admin.auth.admin.createUser` + generated temp password (Resend email) + `profiles.must_change_password=true`; existing emails keep the magic-link fallback. **Migration 195** adds `signup_requests.paid_amount_paise`, `profiles.must_change_password`, self-service `clear_my_must_change_password()` RPC (applied + pg-verified). **E4** frontend forced change: `AuthUser.mustChangePassword` + `fetchAuthSession` mapping + `postLoginPathForSession` guard (single choke point â€” org + staff lanes + signed-in `<Navigate>`) â†’ new `/auth/change-password` view (updatePassword + RPC + land). **E5** EF source-contract tests (`tests/efPaymentPhase.test.ts` 10) + loginRouting gate tests (+3). Gate: tsc âœ“ lint âœ“ vitest 198/2348 âœ“ smoke 387 âœ“ build âœ“ e2e-mock 11/11 âœ“. **âš ï¸ founder action: set `CASHFREE_WEBHOOK_SECRET` in Supabase EF secrets** (matches the Cashfree dashboard webhook secret, `docs/CASHFREE_ONBOARDING.md`) â€” webhook 500s without it. |
| **P-F â€” General issues sweep** | Remaining errors/better-ideas from deep-dives. | F1 sweep â†’ F2 fixes â†’ F3 final re-check â†’ testing loop â†’ release | âœ… **P-F shipped** (2026-08-15, commits `72d66e3` + `233bdf1` + `760c37b`, migration **196** applied live + Deploy/CI green + live 200): **F1** built a **column-drift scanner** (`scripts/check-column-drift.mjs` â€” parses every frontend `.from().select()` incl. embed aliases against live `information_schema`, resolves FK embeds + const-ident selects + count-form selects, flags ambiguous multi-FK embeds) â†’ **F2 fixes** (5 real PGRST204 bugs that silently broke live views): â‘  `monthlyStatementQueries` `po_receipts.project_id` doesn't exist â€” now `po:purchase_orders(project_id)` + `.in("po.project_id", ids)`; â‘¡ `SiteWallKioskView` nonexistent `updates` table + `safety.title` â†’ `site_updates`/`notes` + `safety.description`; â‘¢ `DailySnapshotView` `updates` count â†’ `site_updates`; â‘£ `HandoverPacketView` `projects.slug/started_at/completed_at/address` â†’ `locationâ†’address` etc. Earlier `72d66e3`: `get_signup_for_pay` (mig 196) `plan_amount_inr`/`paid_amount_paise`. **F3 â€” permanent CI gate shipped** (`760c37b`): scanner promoted to a reusable gate â€” `npm run check:columns` + a CI step in `.github/workflows/ci.yml` that self-SKIPs (exit 0) when `SUPABASE_DB_URL` isn't set, so a missing secret never blocks; **hardened scanner + 5 more real drifts it found and fixed**: â‘¤ `LabourKioskView`/`DailySnapshotView` queried a phantom `labour` table (404 PGRST205) â€” rewritten to the real `labour_register` + `attendance` model (kiosk clock-in/out now inserts/updates `attendance` rows, `attendee_kind='labour'` with `labour_id`, today-only list); â‘¥ `deliverableQueries` `req_profiles(name)`/`closed_profiles(name)` bare aliases (400 PGRST200) â†’ `req_profiles:requested_by(name)`/`closed_profiles:closed_by(name)`; â‘¦ `timeQueries` `profiles(name)` ambiguous on time_entries (300 PGRST201, two FKs to profiles) â†’ `profile:profile_id(name)`; â‘§ `platformUsageQueries` `org_members.id` doesn't exist â†’ `profile_id` count. Scanner parser fixes: const-ident `.select(ROW_SELECT)` resolution, count-form `.select("id", {count})`, embed-by-target-name + ambiguity detection. Gate: tsc Â· lint 0 err Â· build Â· smoke **388** Â· vitest **199/2359** Â· e2e-mock 11/11 Â· CI (3 jobs) + Vercel Deploy green Â· live 200. **`SUPABASE_DB_URL` GitHub secret set** (2026-08-15) â€” the CI gate now runs against the live DB on every push to main/prod (previously it printed SKIP until the secret landed). |

### Accepted / deferred (recorded for traceability)
- **B4 â€” real email/WhatsApp delivery**: **email now LIVE** (2026-08-15, migration **198** `notify_delivery_wiring` + `configure-notify-delivery.mjs` + redeployed `notify-deliver` EF v18 `verify_jwt:false`, live 200). The 186 trigger + EF existed but were dead: `app.*` GUCs never set (trigger's `net.http_post(url := NULL)` failed silently), `NOTIFY_INTERNAL_TOKEN` secret absent, deployed EF had `verify_jwt:true` (rejects the trigger's JWT-less call) AND read `profile.email` from `profiles` which has no email column. Fix: migration 198 adds a `notify_config` key/value table (deliver_url seeded, deliver_token via script â€” never committed) + rebuilds `trigger_notify_deliver()` to read config from it with a null-guard; `configure-notify-delivery.mjs` generates the token, sets `NOTIFY_INTERNAL_TOKEN` secret, seeds `notify_config.deliver_token`, records in `.env.local`; EF `fetchProfile` now pulls email via `auth.admin.getUserById`; redeployed with `--no-verify-jwt`. **Verified end-to-end live**: throwaway auth user + profile (email prefs) + `high_issue` email rule + notification insert â†’ `delivered_at` set (pg_net â†’ EF â†’ Resend accepted send) â†’ cleanup done. Gate: tsc âœ“ lint 0 err âœ“ vitest **200/2367** âœ“ smoke 388 âœ“ build âœ“. **WhatsApp/Twilio/push remain deferred** â€” no `WHATSAPP_PERMANENT_TOKEN`/`TWILIO_*`/`PUSH_RELAY_URL` secrets configured; the EF + `whatsapp_client.ts` shells are ready.
- **B6 â€” white-label subdomains / mobile app / AI**: future roadmap. Deferred.
- **P-F follow-up shipped** (2026-08-15, commit `6d9f83e`, migration **197** `kiosk_clock_out` applied live + Deploy/CI green + live 200): the F3-rewritten Labour kiosk's clock-out did a raw `attendance` UPDATE, but `attendance_update` RLS is **PM+ only** â†’ a contractor/site_inspector kiosk operator (who CAN clock in â€” `attendance_insert` is any project member) hit 42501 on clock-out. Fix: **SECURITY DEFINER RPC `kiosk_clock_out(p_attendance_id, p_out_time, p_hours)`** gated by the same membership check (`project_id âˆˆ user_project_ids()`), writes only `out_time`+`hours`, refuses not-checked-in rows (double clock-out â†’ 42501). Direct UPDATE/DELETE stays PM+ (correcting kiosk errors). Frontend `LabourKioskView` now calls the new `clockOutAttendance` helper in `attendanceQueries.ts` (RPC-backed, testable). Verified live: anon 42501 Â· member clock-out OK Â· double clock-out guarded Â· grants authenticated-only. Gate: tsc âœ“ lint 0 err âœ“ vitest **200/2367** âœ“ smoke 388 âœ“ build âœ“ e2e-mock 11/11 âœ“.
- **Frontend Phase 6 mobile polish**: complete per docs/AGENTS.md; the P2 CAD preview modal already uses the responsive `full` Modal size (max-w-5xl) with an overflow-auto render panel.

## 4.6 Track E â€” Remaining Buildable Backlog (2026-08-15)

> Method: same agentic looping pattern â€” per phase â†’ per sub-task: **Deep-Dive â†’ Plan â†’ Build â†’ Verify** â†’ next sub-task; phase re-check â†’ testing loop â†’ release â†’ push live. Lead takes all decisions (user available). Only genuinely-buildable items are scoped; blocked items are recorded as deferred.

| Phase | Scope | Sub-tasks | Status |
|-------|-------|-----------|--------|
| **P-G â€” B6 white-label subdomains** | Per-org branded subdomains (e.g. `<org>.sitetrackpro.in`) that auto-apply org branding on the landing/auth surface and auto-switch the active org post-auth. Buildable: the branding substrate exists (`branding` row, `BrandingEffect`, `useOrgBranding`, `resolveShellBranding`); only the subdomain â†’ org resolution + host routing is missing. | G1 migration **199** (`organizations.subdomain` unique nullable + SECURITY DEFINER `resolve_org_by_subdomain(text)` RPC returning org id + branding row for pre-auth) Â· G2 `src/lib/subdomain.ts` pure hostname parser (`extractSubdomain`, `isWhiteLabelHost`, resolve canonical-vs-subdomain) Â· G3 `src/app/subdomainQueries.ts` (RPC wrapper, client-injected) Â· G4 landing/login branding from subdomain (logo + accent + title pre-auth) Â· G5 post-auth auto-switch active org to the subdomain org Â· G6 tests + smoke + commit | âœ… |
| **P-H - B5 storage quota usage** | Per-org storage usage across the 3 private buckets (deliverables / dpr-media / research-docs) vs plan caps; upload gating via `QuotaGate`/`QuotaMeter` + the `org_quota_snapshot` substrate. | H1 deep-dive bucket paths + quota RPC (done: paths are `<org_id>/.`; quota substrate = `plan_cap`/`org_quota_snapshot`) · H2 migration **200** storage-usage RPC (`storage_usage_by_org` — `storage.objects.metadata.size` by org path prefix, member-gated) · H3 `src/app/storageQuotaQueries.ts` + pure helpers · H4 `QuotaMeter` + upload gates in DeliverablesTab/DrawingsTab/ResearchLibrary/DPRComposer · H5 tests + smoke + commit | ✅ |
| **P-I â€” Testing loop + release** | Full regression with the same loop, then ship. | P-I.1 full gate suite Â· P-I.2 e2e-mock Â· P-I.3 migration apply + push `prod` + live 200 | ✅ |

- **P-H - B5 storage quota**: migration **200** `storage_usage_by_org` had 3 live bugs (`org_id` instead of `p_org_id`, never assigned `bucket`/`used_bytes`/`total_bytes`, no member gate) - fixed in commit `addc279` (rewritten: `p_org_id::text` path match, all 4 output columns assigned per `return next`, `security definer set search_path = public`, member-gated via `user_org_ids()`). Applied live + verified: probe showed `column "org_id" does not exist` before, then clean member-gated `[]` + correct caps (deliverables 50MB / dpr-media 15MB / research-docs 50MB) after. H3 helpers already existed; H4 gates built in `addc279`: `useStorageQuota` hook (`src/hooks/useStorageQuota.ts`), `StorageUploadGate`/`gateFromQuota`/`StorageQuotaWarning` (`src/features/shared/StorageUploadGate.tsx`) wired into DeliverablesTab + DrawingsTab (Attach disabled + warning) and DPRComposer (Submit blocked + `dpr.composer.quotaExceeded` i18n en/hi/te). ResearchLibrary has no file upload (metadata-only form) - gate intentionally not applied. Tests `tests/app/pStorageQuota.test.ts` (10) + smoke +6 markers -> 445 checks. Pushed `prod`; live 200.

- **P-G â€” B6 white-label subdomains**: `migration 199` live applied + verified via probe (`resolve_org_by_subdomain` anon + orgadmin `set_org_subdomain`, subdomain uniqueness index). Code complete: `src/lib/subdomain.ts`, `src/app/subdomainQueries.ts`, `src/features/shell/SubdomainBranding.tsx`, `src/features/shell/useSubdomainBranding.ts`, `src/auth/activeOrgStore.ts`, `src/auth/useAuthUser.ts`, `src/app/AppV3.tsx`, `src/features/org/OrgBrandingView.tsx`. All 202 vitest files / 2387 tests pass. `npm run smoke 388` checks. Column-drift gate PASS. Pushed prod; live 200 OK. Founder DNS action noted: wildcard `*.sitetrackpro.in` CNAME â†’ Vercel.

### Accepted / deferred (blocked, recorded)
- **B4 â€” WhatsApp/Twilio/push delivery**: blocked â€” no `WHATSAPP_PERMANENT_TOKEN`/`TWILIO_*`/`PUSH_RELAY_URL` secrets. EF + `whatsapp_client.ts` shells ready. (Email is LIVE â€” see line 120.)
- **B6 â€” mobile app / AI**: blocked on external infra (mobile build infra, AI provider keys) â€” deferred. Subdomain white-label is now scoped as **P-G** above.
- **P-G founder action (final live wiring, not code)**: add a wildcard `*.sitetrackpro.in` (or chosen base domain) CNAME â†’ Vercel + verify the DNS; the resolver is hostname-driven and degrades gracefully to canonical-host behavior when no subdomain is present.

## 5. Testing Strategy

- Unit: pure helpers in `tests/features/*.test.ts` (vitest, node env).
- Integration: query-layer tests in `tests/app/*.test.ts`.
- Smoke: `scripts/smoke.mjs` curated file+marker scan (currently 382 checks; grows with each phase).
- E2E: e2e-mock suite for auth-guarded journeys.
- Regression: run full gate suite (AGENTIC_SDLC Â§3) at every sub-task close and phase close.
- UAT: client flows via ClientPortal/ClientShare with seeded data; then release.

## 6. Release & Push Live

1. All gates green on `prod` branch. 2. Migrations applied via db:apply. 3. Vercel deploy (project `sitetrack-rakesh`, prod branch `prod`). 4. Post-deploy smoke on production URL. 5. Verify Sentry/logs clean.

## 7. Phase Gating (Definition of Done per phase)

- All sub-tasks committed with clean gate suite.
- Docs updated (AGENTS.md work state, plan status table).
- Phase re-check shows no open risks or accepted-risk notes recorded.
- Release step run only for phases marked ship-ready.
